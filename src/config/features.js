/** Re-enable when the student collaboration flow is ready. */
export const WORK_WITH_US_ENABLED = false;

/** Re-enable when single project detail pages are ready. */
export const PROJECT_DETAIL_PAGES_ENABLED = false;
